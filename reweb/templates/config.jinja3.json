{
    "version": "{{ site.version }}",
    "name": "{{ site.name }}",
    "description": "{{ site.description }}",
    "baseurl": "{{ site.baseurl }}",
    "keywords": [],
    "author": "{{ site.author }}"
}
